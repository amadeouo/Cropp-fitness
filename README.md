# Kropp Fitness — Лендинг фитнес-клуба

Современный, адаптивный лендинг для фитнес-клуба, разработанный в рамках мастер-класса Александра Ламкова. Проект представляет собой полноценную посадочную страницу с современным дизайном и продуманным пользовательским интерфейсом.

Ссылка на мастер-класс: [YouTube](https://youtu.be/AUdW01JQFME?si=y_aRX147P0BZ1oEi)

## 🚀 О проекте

В данном проекте реализована верстка многосекционного лендинга, включающего мотивационные блоки, информацию о тренировках, форму подписки, калькулятор ИМТ (интерфейс) и интерактивную карту.

### Что было реализовано:
- **Header**: Адаптивное меню с состоянием текущей страницы и кнопкой вызова (Book now).
- **Banner**: Главный экран с эффектным заголовком и пагинацией.
- **Motivation**: Секции с чередующимся расположением контента и фоновыми заголовками.
- **Training Types**: Сетка направлений тренировок с использованием CSS Grid.
- **Join Us**: Блок с видео-плеером (превью) и формой регистрации.
- **Location**: Интеграция карты и контактной информации.
- **Family**: Фотогалерея участников клуба с горизонтальным скроллом на мобильных устройствах.
- **Calculate**: Интерактивная форма калькулятора ИМТ с таблицей расшифровки результатов.
- **Footer**: Разветвленный футер с контактами, расписанием, навигацией и социальными сетями.

---

## 🛠 Технологии и методологии

### Стек технологий:
- **HTML5**: Семантическая верстка для лучшего SEO и доступности (использование тегов `main`, `section`, `header`, `footer`, `address`, `time`).
- **CSS3**: 
    - **Custom Properties**: Активное использование переменных для управления цветами, шрифтами и отступами.
    - **Flexbox & Grid**: Современные методы позиционирования элементов.
    - **Clamp & Min/Max**: Адаптивная типографика и отступы без избыточных медиа-запросов.
    - **BEM (Block Element Modifier)**: Методология именования классов для создания независимых и расширяемых компонентов.
- **SVG**: Векторная графика для иконок с возможностью смены цвета через CSS (`currentColor`).

### Принципы разработки:
- **Адаптивность**: Страница полностью оптимизирована под различные устройства (Desktop, Tablet, Mobile).
- **Pixel Perfect (приблизительно)**: Максимальное соответствие дизайну.
- **Performance**: Оптимизация загрузки ресурсов (шрифты в формате woff2, `loading="lazy"` для изображений).
- **Reset/Normalize**: Использование современного CSS-сброса для консистентного отображения в браузерах.

---

## 📂 Структура проекта

```text
.
├── fonts/          # Локальные шрифты (Heebo, Yantramanav)
├── icons/          # Векторные иконки (SVG)
│   └── soc1als/    # Иконки социальных сетей
├── images/         # Растровые изображения (JPG, PNG)
│   ├── family/     # Фото для секции Family
│   ├── motivation/ # Изображения для блоков мотивации
│   └── training-types/ # Иконки типов тренировок
├── index.html      # Основная разметка страницы
├── styles.css      # Основные стили проекта (более 1300 строк)
└── README.md       # Документация проекта
```

---

## 📖 Как запустить

1. Клонируйте репозиторий или скачайте файлы.
2. Откройте файл `index.html` в любом современном браузере.
3. Для разработки рекомендуется использовать расширение **Live Server** (VS Code) для автоматического обновления страницы при сохранении изменений.

---

Проект выполнен: **finished**
